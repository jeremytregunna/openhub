version: '3.8'

services:
  openhub:
    build: .
    container_name: openhub
    ports:
      - "2222:2222"
      - "3000:3000"
    environment:
      - OPENHUB_STORAGE=/data
    volumes:
      - ./data:/data
    restart: unless-stopped
